def square_array(array)
  squared = []
  array.each {|numbers| squared << numbers ** 2}
  squared
  
  
end

#input ---->array of integers
#output ----->array of the squared integers
#each method to square each number in array and return an array of those squared numbers